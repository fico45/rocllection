import 'package:flutter/material.dart';

class Globals {
  static double topSafeAreaPadding = 0.0;
  static double bottomSafeAreaPadding = 0.0;

  static const double formFieldSpacing = 16.0;
  static const double formFieldLabelSpacing = 8.0;

  static final double appBarHeight = AppBar().preferredSize.height;
  static const double spacingBelowFormButton = 18.0;
  static const double itemtemSpacing = 16;
  static const double dynamicModalBottomSheetRadius = 25.0;
  static const double contentHorizontalPadding = 24;

  static final BorderRadius listTileBorderRadius = BorderRadius.circular(8.0);

  static const EdgeInsets listTilePadding = EdgeInsets.all(8.0);
}
